# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Netlify local state
.netlify/

# Node (not used today, but just in case)
node_modules/

# Local backup task log (rewrites itself every 15 min — noise)
tools/backup.log
