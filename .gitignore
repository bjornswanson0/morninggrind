# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Cloudflare / Wrangler local state
.wrangler/

# Node (not used today, but just in case)
node_modules/

# Local backup task log (rewrites itself every 15 min — noise)
tools/backup.log

# Stray/large binaries that don't belong in the web app (Cloudflare rejects >25 MB files)
*.pptx
